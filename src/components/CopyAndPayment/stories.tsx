import { Story, Meta } from '@storybook/react/types-6-0'
import CopyAndPayment from '.'

export default {
  title: 'CopyAndPayment',
  component: CopyAndPayment
} as Meta

export const Default: Story = () => <CopyAndPayment />
