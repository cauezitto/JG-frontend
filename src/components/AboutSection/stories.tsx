import { Story, Meta } from '@storybook/react/types-6-0'
import AboutSection from '.'

export default {
  title: 'AboutSection',
  component: AboutSection
} as Meta

export const Default: Story = () => <AboutSection />
